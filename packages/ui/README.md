# Shared UI boundary

Keep this package empty until the web and mobile screens have a genuinely shared visual language.
React DOM and React Native primitives are different; share tokens and behavior first rather than
forcing platform-specific components into one abstraction too early.

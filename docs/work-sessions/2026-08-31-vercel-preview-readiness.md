- Session: Vercel preview readiness snapshot
- Date/duration: 2026-08-31; about 1 hour
- Scope/start: Recorded `READY FOR VERCEL PREVIEW` before any Vercel build at base revision `71c6fabc148992a9773a34c5666f3fdbf149e606`; the worktree had two intended tracked edits plus untracked root `vercel.json`, with no commit, project link, deployment, promotion, or domain connection.
- Changes: Reset `.install-footer-link` to a transparent background while preserving button semantics and shared focus/hover/disabled behavior; excluded `pwa.spec.ts` only from the development-server Playwright config; retained the reviewed root Vercel build/output/SPA-rewrite configuration unchanged.
- Decisions: Preserve GoatCounter and `/app/dashboard?created=<encoded custom menu name>` for this release; use repository root, Vite auto-detection, automatic install, `pnpm build`, `apps/web/dist`, and `/(.*)` to `/index.html`, relying on filesystem precedence for manifest/icons/service-worker/Workbox/JS/CSS; add no CSP, Functions, cache headers, dependencies, or app changes.
- Roadblocks: Vercel's automatic pnpm table stops at 10 while the repository pins 11.22.0, so set `ENABLE_EXPERIMENTAL_COREPACK=1` for Preview and Production and confirm pnpm 11.22.0 plus a compatible Node version in the eventual preview build log; this is a dashboard prerequisite, not a code failure.
- Verification: Footer 4/4 and accessibility 36/36 passed, including all 18 former contrast cases; general discovery/full E2E passed 118/118 with no six-project PWA duplicates; dedicated production PWA passed 3/3; `pnpm verify:full` passed domain 60/60, store 54/54, web 103/103, clean build/12-entry precache; `pnpm audit --prod` found no vulnerabilities; only accepted NO_COLOR/FORCE_COLOR warnings appeared; diff was clean with no tracked dist, env files, secrets, or debug logging.
- Follow-up: Commit this exact reviewed worktree including `vercel.json`, create/link a Vercel project at repository root, add the Corepack variable to both scopes, create only a Preview deployment, and verify the build log and generated routes/assets before any separately authorized production promotion or custom-domain connection.

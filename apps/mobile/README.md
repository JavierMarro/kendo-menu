# Mobile app placeholder

The Expo app will be initialized here after the shared domain and store contracts settle. Expo
supports pnpm workspaces, so it can consume `@kendo-menu/domain` and the platform-neutral parts of
`@kendo-menu/store` without introducing a second package manager.
